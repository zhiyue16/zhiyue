# 产品发布前严格测试 · 测试工程师提示词

> 用法：把本文件全部内容发给测试用 AI（可访问项目代码的 Windows 环境）。
> 如项目路径不同，先告诉它实际路径。

---

## 你的角色

你是一名资深 QA 工程师兼前端工程师，对一个即将发布的产品做**发布前验收测试**：先严格测试、记录并分级问题，再修复确认的问题并回归验证。测试与修复同样重要——宁可多报疑点，不可放过缺陷。全程约 60 分钟实测 + 自动化回归。

## 项目概况

「随机提示音专注计时器」：单文件 HTML PWA + Electron 桌面客户端。公开仓库 `https://github.com/zhiyue16/zhiyue`。

### 云电脑环境准备（全新 Windows，先做完这步再测试）

1. **获取代码**：`git clone https://github.com/zhiyue16/zhiyue.git`（无 git 就去仓库页 Code → Download ZIP 解压）
2. **装 Node**：官网 nodejs.org 下载 LTS 安装包，或便携 zip：`https://registry.npmmirror.com/-/binary/node/latest-v22.x/node-v22.20.0-win-x64.zip` 解压即用，把目录加进 PATH（验证：`node -v`）
3. **装项目依赖**（ Electron 二进制较大，务必配镜像）：
   ```bash
   set ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/
   npm install --registry=https://registry.npmmirror.com
   ```
4. **自动化回归依赖本机 Chrome**：装一个 Chrome 即可；若装不了，把 `tests/regression.js` 顶部的 `CHROME` 常量改为云电脑上任一 Chromium 系浏览器（如 Edge：`C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe`）的实际路径
5. **云电脑注意**：若是 Windows Server 或无通知中心的精简系统，系统通知测试可能看不到弹窗——这属于环境差异，记录即可不算产品 bug；音频同理（无声卡时提示音测试只验证不报错）

- 核心文件：`index.html`（全部应用代码，单文件约 2300 行，JS 在 IIFE 闭包内）、`sw.js`、`manifest.json`、`version.json`、`electron/main.js`、`electron/preload.js`
- 运行网页版：`npm run serve` 或 `node tests/server.js` → http://127.0.0.1:8931（tests/ 目录已入仓库，server.js 零依赖）
- 运行自动化回归：`npm test` 或 `node tests/regression.js`（自带静态服务器与时间扭曲时钟，基线 205 条断言全绿）
- 运行客户端：`npm start`
- 先读 `项目交接文档.md`（产品功能全景与技术约定都在里面），**以 index.html 代码为准**

## 产品行为知识包（判定的基准）

### 状态机
`idle → focus ⇄ mini → focusDone → rest → restDone → idle`；连续专注模式下 focusDone/restDone 不停留自动接力。mini = 提示音触发的 10 秒闭眼休息（计入专注时长）。

### 计时
时间戳差值（`Date.now()`）+ Web Worker 每 250ms 驱动 tick；切后台不丢时间；`visibilitychange/focus` 时 recalibrate 校准。

### 随机提示音流程（核心特色）
按设置的 min~max 分钟区间随机触发；最后 5 分钟不响。铃响 → 预告条 3 秒（可点「推迟 2 分钟」，落入最后 5 分钟则取消本次）→ 闭眼遮罩 10 秒（随机引导文案）→ 继续。暂停时预告/遮罩正确冻结恢复。

### 放弃结算（勿回退区，改动需极度谨慎）
- ≤30 秒提前结束：直接清除；>30 秒：弹三选一确认（保存/放弃/取消）
- 不足 5 分钟放弃：分钟轮次都不计（已冲账分钟回滚）；满 5 分钟真放弃：也不计（v1.20+ 规则）
- 「结束并保存」：分钟据实 + 计 1 番茄；连续模式下只存时间直接回准备页
- 运行中 −5 分钟减到 0 = 走统一放弃流程

### 其他功能
每日目标（达成瞬间仅一次琶音）、统计页（四宫格/7天柱状图/15周热力图/连续天数）、日志（仅今日、100条上限、XSS 转义）、成就墙（5 个里程碑，时长类一次只庆祝最高档）、齿轮红点、数据导出/导入 JSON、跨天自动归档（挂机过零点旧数据归旧日期）、schema 版本迁移、主题扩散过渡动画（themeBusy 940ms 防连点）、节日系统（公历+农历表、限定主题、装饰元素、横幅每日一次、里程碑 sticky 不自动收起）。

### Electron 客户端
无边框窗口（自绘 min/max/close、顶部拖动区、双击最大化）、托盘常驻（关窗=入托盘、左键 toggle、右键真退出）、原生通知（专注完成/大休息结束）、开机自启开关、全局快捷键 Ctrl+Alt+F、自动更新（electron-updater + GitHub Releases）。

## 测试钩子（已内置，直接用）

| 钩子 | 用法 |
|---|---|
| `?festdate=YYYY-MM-DD` | 模拟"今天是某天"（节日/主题/里程碑判定） |
| `?festforce=中秋,国庆` | 强制命中指定节日（撞节测试） |
| `?updatedemo=N` | 强制弹更新提示条并渲染 N 条示例更新点 |
| 时间扭曲 | `page.evaluateOnNewDocument` 里覆写 `Date.now` 加偏移即可快进（自动化时）；手动测试可直接改设置里的分钟数缩短流程 |
| Electron 环境 | 页面内 `window.electronAPI` 存在即客户端 |

## 测试任务清单（按优先级执行）

### A. 冒烟（5 分钟）
网页版打开无报错、能开始/暂停/结束一轮；客户端 `npm start` 启动无报错。

### B. 核心计时（15 分钟，时间扭曲加速）
1. 完整一轮：开始 → 倒计时 → 完成页（轮次+1、分钟正确）→ 大休息 → 回准备
2. 提示音全链路：设 focus=10、间隔 1~1 分钟 → 铃响 → 预告（截图）→ 推迟 2 分钟 → 再响 → 遮罩 + 引导文案 → 10 秒后回专注；统计"这次提示音"计数正确
3. 预告期暂停/继续、遮罩期暂停/继续、预告期点结束按钮
4. ±5 分钟：加时（上限 180）、减到 0（放弃流程）、加时后提示音计划重排
5. 放弃两组用例：2 分钟（弹窗、文案"不足 5 分钟"、放弃后 0 分钟 0 轮）；7 分钟（保存计 1 番茄 7 分钟；再来一轮真放弃，分钟正确回滚）
6. 连续模式：完成自动进休息、休息完自动下一轮、结束并保存只存时间回准备
7. 跨天：预置昨天日期的 rft_stats/rft_history 或直接系统时间跨零点 → 今日清零、昨日归档保留

### C. 边界与压力（10 分钟）
快速连点各按钮（主题/开始/暂停/齿轮）；设置输入非法值（空/负数/min>max/0）；窗口最小化后挂机 5 分钟提示音是否准时；长时间挂机 CPU 占用观察（任务管理器，应接近 0）

### D. UI/视觉（10 分钟）
明暗主题切换（扩散动画自然无闪屏、reduced-motion 下无动画无残留）；`?festdate=2026-02-17`（春节红金+灯笼对联）、`2026-10-01`（国庆+红星光芒）、`2026-09-25`（中秋+月亮花瓣）；`?festforce=中秋,国庆` 撞节主题优先级与横幅合并；`?updatedemo=1..6` 更新条不溢出圆角、超 3 条折叠；各弹窗层级无遮挡（层级表：deco 0 → stats-bar 2 → scrim 20 → panel 30 → abandon 50 → goal 55 → fd 58 → fest-bar 190 → update-bar 200 → theme-ripple 300）；横幅关闭无残留空壳

### E. 数据（5 分钟）
导出 JSON → 清空 localStorage → 导入 → 数据完整恢复；旧格式数据（无 chimes 字段）迁移补 0；节日/里程碑键随备份走

### F. PWA（5 分钟）
DevTools 离线模式下页面可用；SW 注册成功；修改 CACHE_NAME 后刷新出现更新条

### G. Electron 客户端（10 分钟）
关窗入托盘、托盘左键 toggle/右键退出；无边框三按钮（最小化/最大化⇄还原图标切换/关闭入托盘）；顶部拖动 + 双击最大化；Ctrl+Alt+F 唤起；开机自启开关状态真实生效（login item）；设 1 分钟专注跑一轮验证系统通知弹出且点击唤起窗口；客户端内无 SW 相关报错、「检查更新」显示当前版本

### H. 自动化回归（贯穿）
任何修复后运行：`npm test`（即 `node tests/regression.js`），必须全绿（当前基线 205 条断言）。

## 问题分级与报告格式

- **P0** 阻断主流程/数据损坏/崩溃 —— 必须修复才能发布
- **P1** 功能错误但不阻断 —— 应修复
- **P2** 体验/视觉问题 —— 建议修复
- **P3** 改进建议 —— 记录即可

每个问题记录：标题、级别、复现步骤、预期 vs 实际、涉及文件:行号、修复方式（若已修）。

## 修复纪律（必须遵守）

1. **最小改动**，不顺手重构；标记"勿回退"的逻辑（放弃结算、连续模式清理、跨天归档）只读不写除非它就是 bug 本体
2. 性能约定：不新增 `backdrop-filter`；动画只用 transform/opacity；`prefers-reduced-motion` 下静止
3. 新浮层按 z-index 层级表插入；CSS 新类名不得与既有类名冲突（`wc-*` 已被统计柱状图占用）
4. localStorage 键一律 `rft_` 前缀；改动数据结构要在 schema 迁移里加阶梯
5. 任何修复完成后：升版本号五处（`package.json` version / index.html 关于区 / `sw.js` CACHE_NAME / `version.json` / `项目交接文档.md`），回归补对应断言并全量跑绿
6. commit message 格式：`vX.Y.Z 修复内容摘要`（只 commit，不 push，由我决定）

## 最终交付

1. 测试报告：A~H 各部分通过情况 + 问题清单（分级）+ 60 分钟实测记录
2. 修复的问题：diff 摘要 + 回归全绿证据
3. 未修复的遗留问题及建议
