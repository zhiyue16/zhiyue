# 随机提示音专注计时器 · 项目交接文档

> 用途：把本项目搬到新对话继续优化时，将本文档（连同源代码压缩包）发给新会话，即可完整恢复上下文。
> 生成日期：2026-08-14 · 当前版本 **v1.25.1**

---

## 一、项目是什么

一个**单文件 HTML PWA 专注计时器**：绿色专注圆环倒计时，专注期间随机播放提示音，听到提示音后弹出 10 秒闭眼休息遮罩（这 10 秒计入专注时长），支持手动大休息、±5 分钟悬浮微调、6 种合成音色、完成时播放 TickTick 风格 E6→G6→C7 三音、设置/日志面板、localStorage 统计、明暗双主题、音量调节、本轮小结、放弃确认弹窗、主题切换涟漪动画。

- **线上地址**：https://zhiyue-zeta.vercel.app/
- **GitHub 仓库**：`zhiyue16/zhiyue`（push 到 main → Vercel 自动部署，几秒上线）
- **部署平台**：Vercel Hobby 免费计划（个人非商用永久免费）
- **作者署名**：纸月十六

## 二、长期约定（必须遵守）

1. **交付的网页文件必须命名为 `index.html`**（用户明确要求"以后都是这样"）。
2. 更新上线方式：用户自己把 `index.html`（及 `sw.js`，若缓存版本变了）上传到 GitHub `zhiyue16/zhiyue` 仓库，Vercel 自动部署。**不要索要或代操用户的 GitHub 账号**。
   - **例外（用户已主动授权）**：本会话通过用户提供的 fine-grained PAT + GitHub Contents API 直接从沙盒推送（GET 取 sha → PUT base64 内容），v1.9.0 起所有版本均由此自动部署，无需用户手动上传。令牌权限曾给得偏宽（admin/push），已建议用户改为 Contents-only 后重新生成；**新 AI 若要沿用此方式，须用户重新粘贴一次令牌**（不要记录在任何文件里）。
3. 每次改功能要同步做：版本号 +「关于」里的日期、sw.js 的 `CACHE_NAME`（否则旧缓存不更新）、**version.json 的版本号/日期/更新说明**（v1.16.0 起，更新提示条的数据源）、回归测试。
4. 视觉主题（配色、背景、圆环样式等视觉表现）不属于"勿回退"的保护范围，用户明确提出视觉风格需求时应当大胆实现，以达到设计目标为优先。性能约定（不加 backdrop-filter、动画只用 transform/opacity）仍然适用，但"谨慎"不等于"只改边角料"——换肤、节日主题这类需求，背景、圆环、按钮、卡片等主要视觉元素都应按设计目标调整，而不是只改一两个小元素。

## 三、文件清单

| 文件 | 作用 |
|---|---|
| `index.html` | 全部应用代码（HTML+CSS+JS 单文件，约 87KB，v1.14.1） |
| `sw.js` | Service Worker，`CACHE_NAME = 'focus-timer-v42'`，离线缓存、仅缓存 GET、导航请求回退 index.html、支持「检查更新」提示条；`version.json` 不经缓存直连网络 |
| `version.json` | 当前版本号 + 日期 + 更新说明（v1.16.0 新增）。更新提示条弹出时实时拉取，**发版必改** |
| `manifest.json` | PWA 清单 |
| `icon-192.png` / `icon-512.png` | PWA 图标（根目录扁平路径） |

> 站点已接入**百度统计**（v1.14.3，代码在 `index.html` `<title>` 下方，站点 ID `d1a26a6ccef8d3bbef10527858ae62b2`）：实时访客/在线人数在 tongji.baidu.com 后台查看。
| `说明.md` | 源码交接说明（面向第三方开发者） |

源代码打包：`专注计时器-源代码.zip`（7 个文件：index.html / sw.js / manifest.json / icon-192.png / icon-512.png / 说明.md）。**新对话请直接上传此 zip + 本文档。**

## 四、当前功能全景（v1.14.1）

- 专注计时：圆环进度、剩余时间显示、时间戳计时（`Date.now()` 差值，不受后台节流影响）
- 随机提示音：按设置区间随机触发，触发后弹 10 秒闭眼遮罩，闭眼时长计入专注
- 手动大休息：focusDone → rest → restDone 流程；连续专注模式（v1.13.0，设置-时长里的开关，默认关）：focusComplete 直接 startRest、restComplete 直接 startFocus 自动接力，一直循环直到手动结束
- 时长微调：界面上 ±5 分钟按钮；点击时间数字弹「专注时长设置」弹窗（v1.14.0，仅 idle 可开，5~180 钳制，Enter/Esc 支持；v1.14.1 改为挂在时间下方的小浮层、无全屏遮罩）
- 音量控制：设置面板滑块（音色下方、网站信息上方），`masterGain` 总线，静音切换记住上次音量
- 本轮小结：专注完成时快照本轮时长/提示音次数/闭眼秒数
- 放弃确认（番茄 ToDo 风格）：>30 秒弹确认框，≤30 秒直接清除；<5 分钟不计分钟数和轮次，≥5 分钟计分钟不计轮次
- 主题切换形变动画（v1.11.0）：日夜按钮内嵌 SVG 太阳⇄月亮形变（月缺滑入、光芒收起旋转淡出，.45s 缓出曲线），由 data-theme 驱动纯 CSS transition，无 JS 动画帧
- 微动画：仅 transform/opacity（GPU 合成），`prefers-reduced-motion` 全部禁用
- 每日目标完成音效（v1.9.0）：`playGoalDone()` 五音上行琶音，只有从 `goal-1` 跨到 `goal` 那一轮才响，防重复
- 日志：按天筛选、XSS 转义（escHtml）、日期字段；上限 100 条超出截断
- 统计：localStorage 持久化累计分钟/轮次
- 统计页（v1.6.0 新增）：面板第三个标签「统计」，番茄ToDo 式概览四宫格（今日/总番茄、今日/总专注时长、与前一天对比）、近 7 天专注柱状图、累计天数/连续天数/平均番茄/单日最佳/累计提示音。数据源 `rft_history`（按天归档 {minutes,rounds,chimes}，saveStats 时同步写入），`stats.chimes` 统计提示音次数
- 每日目标（v1.8.0）：主页番茄胶囊显示 `cur/goal`，点击弹步进器设置（1~50 个）；完成瞬间播放 `playGoalDone()` 五音琶音，只有"刚好跨过目标线"那一次响
- PWA：可安装、离线可用；设置-关于区有「检查更新」按钮（v1.12.0），发现新版自动弹出顶部更新条
- **v1.22.x 桌面布局**（用户明确无需手机端适配，桌面唯一标准；v1.22.2 定稿右侧成组）：
  - 顶部条 = 紧凑药丸（v1.22.0 形态）：`width:fit-content`；v1.22.2 起在**右上** `margin:22px 96px 0 auto`（96px 是给灯笼留的悬挂位）
  - 设置面板 = 药丸正下方**右对齐成组**：`right:96px; top:120px; transform-origin:top right`，translateX(16px) 滑入；top:120px 是给右上月亮/星星装饰让位
  - **联动校对约定：药丸或面板位置变化时，灯笼（`.lantern-l right:414px` / `.lantern-r right:26px`）、`.moon`（top:16px right:3%，中秋光晕 94% 7% 对齐）、`.star.s3`（top:48px）、`.star.s4`（right:36%）、festBar/updateBar 位置必须全部重新校对**（回归 tLayout 套件有 1440/1280 双视口位置断言）
- **v1.15.0 新增**：
  - 提示音预告：铃响 → 预告条 3 秒（可「推迟 2 分钟」）→ 才弹闭眼遮罩（原铃响即弹过于突然）；状态变量 `previewEndAt/previewLeft`，常量 `PREVIEW_MS/POSTPONE_MS`，预告条 id `#chimePreview`（不用 backdrop-filter，遵守性能约定）
  - 闭眼遮罩引导文案：`MINI_TIPS` 随机一句（`#miniTip`）
  - 数据备份：设置-关于区「导出数据 / 导入数据」，`app:'random-focus-timer'` 校验，导入后 reload 走完整启动+迁移流程
  - 统计页近 15 周热力图（GitHub 风格，30/60/120 分钟分四档，`#heatGrid`）
  - Web Worker 驱动 tick（Blob 内联，后台标签页不被节流；不支持 Worker 回退主线程 setInterval）
  - localStorage schema 版本迁移：`rft_schema` 键 + 启动时 `migrateSchema()` 阶梯迁移
  - iOS 回前台 `actx.resume()` 修切后台无声；`navigator.storage.persist()` 防存储驱逐
  - 百度统计接入（v1.14.3，站点 ID `d1a26a6ccef8d3bbef10527858ae62b2`）
- **v1.17.0 新增（节日庆祝 & 里程碑）**：
  - 节日表在 `index.html`「节日庆祝 & 个人里程碑」区块：`SOLAR_FESTIVALS`（MM-DD 固定）+ `LUNAR_FESTIVALS`（2026-2030 公历日期手动维护，已双源核实；**每年底补充下一年日期到对应 dates 数组**）
  - 节日横幅 `#festBar`（z-index 190，错位 top:70px 避开 update-bar；无 backdrop-filter）：点击或 8 秒收起；`rft_fest_shown` 记当天已弹
  - 限定配色：春节/国庆/中秋挂 `html[data-fest]`。v1.18.0 重做：每节一套完整变量包（渐变背景 body background + 文字/accent/pill 等 10 个变量 + 按钮单独覆盖金底深字），春节红金灯笼光晕/国庆旗红星辉/中秋夜空明月，深浅色分别适配，文字对比度 WCAG AA；节日后自动摘除。状态栏 theme-color 由 `applyFest()` 跟随（`FEST_META`），主题切换后也会重刷
  - 节日装饰（v1.19.0）：`.deco-spring/.deco-national/.deco-midautumn` 常驻 DOM 默认 `display:none`，data-fest 激活显示；全部 fixed + `pointer-events:none` + z-index 0（与 blob 同层，低于 main:1/topbar:2）；纯 CSS/SVG 无图片；动画仅 transform/opacity（灯笼 rotate 摇曳/星星 opacity 闪烁/花瓣 translate 飘落/光芒射线静态）；reduced-motion 由全局规则静止；灯笼 <1240px 隐藏、对联 <1100px 隐藏；装饰元素不得加交互，回归有「不遮挡开始按钮」断言
  - 里程碑（v1.21.0 重构）：`MILESTONES` 统一定义表（id/名称/描述/cheer/test/progress）是判定与进度的唯一数据源，`checkCelebrations` 与成就墙 `renderAchv()` 共用，**勿另写判定逻辑**；统计源 `milestoneStats()` 从 rft_history 实时计算（累计分钟/轮次/陪伴天数）。`rft_milestones[id]` 存达成日期字符串（老数据 true 视同已达成无日期）；`_seen` 数组记已查看。时长类一次只庆祝新达成最高档、低档并标记。达成时播 `playMilestone()`（G4→C5→E5→G5 上行 + C6/E6/G6 终止式和弦），横幅 sticky 须手动关闭（节日横幅仍 8 秒自动收起）；齿轮 `#unreadBadge` 红点由 `updateBadge()`/`markAchvSeen()` 维护，进成就页消除
  - 触发时机=启动+跨天+分钟冲账（flushFocusMinutes）+轮次进账（bumpRound）
  - 撞节规则（v1.20.0）：`todayFestivals()` 返回数组，主题优先级=带 theme 农历 > 带 theme 公历 > 无 theme，祝福全部合并横幅；测试钩子 `?festforce=中秋,国庆` 强制命中
  - 农历表过期提醒（v1.20.0）：`LUNAR_MAX_YEAR` 从 dates 自动推算，超出当前年份 console.warn；关于区 `#lunarCover` 常驻显示覆盖年份
  - 命中多条合并一条横幅（.fb-line 多行）；测试钩子 `?festdate=YYYY-MM-DD` 模拟当天
  - 导出/导入已含 `rft_fest_shown`、`rft_milestones`

## 五、关键技术要点（改动时必读）

### 状态机
```
idle → focus ⇄ mini → focusDone → rest → restDone → idle
（连续模式：focusComplete→rest→startFocus 自动接力，见下）
```
（mini = 提示音触发的 10 秒闭眼状态）

### 连续专注（v1.13.0）
- 开关存 `cfg.continuous`，UI 是设置里的 .switch 滑块（role=switch）
- 自动流转：focusComplete 检测到开启则不落 focusDone、直接 startRest()；restComplete 直接 startFocus()
- 提前结束：连续模式下 settleAbandon(true) 只存时间不走路、直接 toIdle（不计番茄不进循环）；rest 中"提前结束休息"按钮仍走原 doneBtn 逻辑
- startRest 里要清 chimeAt/chimeLeft，避免上一轮的提示音计划串到新状态

### 计时
- v1.15.0 起 tick 由 Blob 内联 Web Worker 每 250ms postMessage 驱动（后台标签页不被节流），不支持 Worker 回退主线程 `setInterval`；时间戳差值计时；`visibilitychange`/`focus` 时 `recalibrate()` 校准
- `focusAccMs` 累计已暂停结算的专注毫秒；分钟数满 1 分钟即"冲账"进 `stats.minutes`
- ±5 分钟微调：`adjust(deltaMin)` 在 focus/rest 运行中直接改 `endAt`；加时有 180 分钟上限；focus 减到 0 会走 `requestAbandon()` 统一放弃流程（按已专注时长决定弹窗/统计），rest 减到 0 直接回 idle

### 数据层（localStorage 四把钥匙）
| key | 结构 | 说明 |
|---|---|---|
| `rft_cfg` | `{focus, minInt, maxInt, rest, sound, theme, volume, goal, continuous}` | 设置。focus 5-180 / minInt·maxInt 1-30 / rest 1-120 / sound 6 种 / theme light·dark / volume 0-100 / goal 1-50 / continuous bool |
| `rft_stats` | `{date, minutes, rounds, chimes}` | 今日累计。date 变（跨天）时由启动时的 `saveStats()` 重置并归档；v1.14.2 起挂机跨天也会由 `checkDayRollover()` 自动归档清零 |
| `rft_history` | `{"YYYY-MM-DD": {minutes, rounds, chimes}, ...}` | 按天归档，统计页数据源。`syncHistory()` 在每次 `saveStats()` 时把今日数据写入；`pruneHistory()` 只留最近 730 天 |
| `rft_logs` | `[{date, time, type, text}, ...]` | 日志，最新在前，上限 100 条（超出截断） |

### 放弃结算（v1.4.0 修过大坑，勿回退）
- `ABANDON_CONFIRM_MS = 30000`，`RECORD_MIN_MS = 5*60000`
- `roundBaseMins`：开始专注时记下 `stats.minutes` 基线
- `elapsedFocusMs()` = `(stats.minutes − roundBaseMins)*60000 + focusAccMs + (运行中则加 now − lastTick)`
- `settleAbandon`：≥5 分钟只补"未冲账的零头"（`totalMins − flushed`），<5 分钟回滚已冲账分钟
- 历史 bug：曾误扣历史分钟、曾漏读已冲账分钟导致 2 分钟放弃不弹窗——都已修，回归测试覆盖

### 音频
- Web Audio API，`master()` 懒初始化 `masterGain`，**所有合成器必须 `.connect(master())`**，音量 = `cfg.volume/100`
- 完成三音：频率 [1318.5, 1568, 2093]，间隔 0.38s

### 主题形变（v1.11.0 替代旧涟漪方案）+ 扩散过渡层（v1.23.0）
- 按钮内嵌 `#themeIcSvg`：`.tm-core`（太阳圆盘，套 mask 挖出月牙缺口）、`.tm-cutout`（mask 里的遮罩圆）、`.tm-rays`（8 根光芒）
- 深色态 CSS：core `scale(1.15)`、cutout `translate(-13px,5px) scale(.62)`、rays `scale(0) rotate(90deg)` 淡出；浅色态全部复位
- transition `.45s cubic-bezier(.25,.9,.35,1)`（缓出无过冲），`prefers-reduced-motion` 下禁用
- JS 侧：`themeBusy` 防抖（v1.23.0 起 940ms，覆盖完整扩散动画；reduced-motion 分支仍 460ms），动画期间吞掉连点；`cfg.theme` 点击瞬间即落盘（动画中途刷新主题也正确）
- **扩散过渡层 `#themeRipple`**（z-index 300，层级表最高层）：点击后以按钮中心为圆心，软边圆（`radial-gradient(circle, COLOR 60%, transparent 78%)`）`scale(0)→覆盖整屏`（.62s），380ms 时在圆层遮盖下 `applyTheme()+applyFest()`，660ms 淡出，940ms 复位解锁。颜色表 `RIPPLE_BG` 含三个节日变体（fest 激活时用节日底色扩散）。reduced-motion 下 JS 直接跳过扩散走原路径，无残留

### 手动检查更新（v1.12.0）
- `swReg`、`showUpdateBar` 提升到了 IIFE 顶层作用域（原来在 if 块内），供检查更新按钮复用
- 流程：`reg.update()` → `reg.waiting` 且页面受控 → 发现新版本；`reg.installing` → 等 statechange，8s 超时兜底；都没有 → 已是最新
- 判断"更新"必须满足 `navigator.serviceWorker.controller` 存在，否则是首次安装缓存，不能弹更新条

### 弹窗层级（z-index 从低到高）
`deco 节日装饰`(0) → `stats-bar`(2) → `scrim`(20) → `panel`(30) → `abandon-mask`(50) → `goal-mask`(55) → `fd-mask`(58) → `fest-bar`(190) → `update-bar`(200)
新加弹窗时按此插入，别乱放。updateBar 显示期间 festBar 加 `.lower` 下移避让（`syncFestBarPos()`，v1.20.0）。

### 性能结论（已实测）
- 动画本身 GPU 便宜；真正掉帧来源是 `backdrop-filter` 模糊——**不要再加新的 backdrop-filter**
- 计时精度不受动画影响

### 更新提醒条（update-bar）
- SW 安装完成且页面受控时弹出（v1.10.0 新增），用户点「立即更新」→ `postMessage({type:'SKIP_WAITING'})` → 新 SW 接管 → `controllerchange` 刷新
- v1.16.0：弹出时 `showUpdateBar()` 实时拉取 `version.json`（cache-busting + `cache:'no-store'`）展示新版本号与更新说明列表；**离线/拉取失败降级为只显示「发现新版本」**，按钮流程不受影响。`version.json` 在 sw.js 里被排除出缓存拦截（否则缓存优先策略会喂回旧说明），也不进预缓存清单
- v1.17.1：气泡圆角由 100px 改固定 22px + `overflow:hidden`（多行时大圆角切掉首末行的溢出 bug）；更新点最多 3 条、超出折叠「等 N 项更新」，每条单行省略；测试钩子 `?updatedemo=N` 强制弹出并渲染 N 条示例更新点
- v1.17.2：fest-bar / update-bar 收起态加 `visibility:hidden`（延迟到滑出动画结束后生效）——translate 百分比相对自身高度，空壳高度小时上移后仍露出一条残留；**以后新加"滑出屏幕"式收起组件都要走这个模式，不能只用 translate**
- `reg.waiting` 是"已下载好、等激活"的新 SW；`reg.installing` 是"还在下载"的新 SW
- 30 分钟定时 `reg.update()` + 每次打开页面浏览器自动查一次，覆盖长时间挂机场景

### 代码结构注意
- 全部 JS 在 IIFE 闭包里：浏览器自动化测试时**无法** `evaluate` 访问内部函数，只能 DOM 断言
- 主按钮 id 是 `#startBtn`（不是 #btnStart）
- 全部 DOM 缓存对象 `dom` 共 18 个元素，新加需要频繁读写的元素记得加进去
- 第三方优化已合并：DOM 缓存（`dom` 对象 18 个元素）、`num()` 输入校验（0 是合法值，不能 `||dflt`）、`todayKey()` 手动拼 YYYY-MM-DD、theme-color meta 同步、iOS meta、Firefox range-track、aria 无障碍、标题按需写入

## 六、待办 / 下一步

- [x] v1.14.1 已由 AI 通过 GitHub API 推送（index.html + sw.js，CACHE_NAME=focus-timer-v24），Vercel 自动部署
- [ ] 后续优化方向（用户在新对话中提出）

## 七、Electron 桌面客户端（v1.24.0 新增）

### 文件清单
| 文件 | 作用 |
|---|---|
| `package.json` | Electron 应用配置：`main: electron/main.js`，scripts `start`/`dist`，build（electron-builder NSIS，appId `com.zhiyue.focustimer`） |
| `electron/main.js` | 主进程：单例窗口（show:false/ready-to-show/backgroundThrottling:false）、托盘（close→hide、左键 toggle、右键真退出）、原生通知（`rft:notify` IPC）、开机自启（`rft:loginItem:*`，login item）、全局快捷键 Ctrl+Alt+F（注册失败静默降级）。模式参考 Stretchly：`window-all-closed` 空函数 + `setAppUserModelId(appId)` |
| `electron/preload.js` | contextBridge 暴露 `window.electronAPI`（notify/getAutoLaunch/setAutoLaunch）；**该对象存在即 Electron 环境标志** |
| `build/icon.png` | 打包图标（icon-512.png 副本，electron-builder 约定路径，自动生成 .ico） |
| `node_modules/`、`release/` | 依赖与打包产物，已入 .gitignore |

### index.html 的 Electron 适配（特性检测，浏览器零影响）
- `isElectron = typeof window.electronAPI !== 'undefined'`；Electron 下：SW 注册静默跳过、「检查更新」行隐藏、「开机自动启动」开关显示（状态存主进程 login item，不进 rft_cfg）
- `nativeNotify('focus'|'rest')` 在 `focusComplete`/`restComplete` 各一行，浏览器为空操作；提前结束保存不发通知

### 本地打包命令（便携 node，全程镜像）
```bash
export PATH="$PWD/.tools/node:$PATH"
export ELECTRON_MIRROR="https://npmmirror.com/mirrors/electron/"
export ELECTRON_BUILDER_BINARIES_MIRROR="https://npmmirror.com/mirrors/electron-builder-binaries/"
node ./.tools/node/node_modules/npm/bin/npm-cli.js install --registry=https://registry.npmmirror.com
node ./.tools/node/node_modules/npm/bin/npm-cli.js start   # 开发模式跑起来
node ./.tools/node/node_modules/npm/bin/npm-cli.js run dist # 打包 → release/ 下的 NSIS .exe
```

### 发布同步流程（v1.25.1 起，GitHub Actions 全自动）
1. 改代码 → 升版本号**五处**：`package.json` version、index.html 关于区、sw.js `CACHE_NAME`、version.json（version/date/notes）、本文档
2. `git commit && git push` → Vercel 部署网页版
3. `git tag vX.Y.Z && git push origin vX.Y.Z`（tag = "v" + package.json version）
4. **结束**。`.github/workflows/release.yml` 自动触发：云端 Windows `npm ci` → electron-builder 打包 → 创建同名 Release → 上传 exe / latest.yml / exe.blockmap（electron-updater 数据源，三者由 `--publish always` 自动上传，无需手动）
5. 旧版客户端启动时或点「检查更新」→ 发现新版 → 通知 → 下载 → 重启升级
- 权限：workflow 里已声明 `permissions: contents: write`，公开仓库默认即可；若 run 报权限错误 → Settings → Actions → General → Workflow permissions → Read and write permissions
- 本地仍可用 `npm run dist` 手动打包（加 `--publish always` 则等同云端行为，需 GH_TOKEN）

### 自动更新机制（v1.25.0）
- `electron-updater`（dependencies，随 app 打包）+ `build.publish` 指向 GitHub 仓库；`autoDownload=false`（用户确认才下载）
- main.js 事件：available → 通知「发现新版本，点击下载」；downloaded → 通知「更新已就绪，点击重启升级」；进度/状态经 `rft:update:status` 转发渲染进程
- 关于区「检查更新」按钮：Electron 下由适配块接管（状态机 idle/checking/latest/available/downloading/ready/error/dev）；**网页版 SW 检查更新 IIFE 已加 `!isElectron` 守卫**，两套逻辑互不干扰
- **静默降级**：断网/GitHub 不可达/dev 模式（无 app-update.yml，isPackaged 守卫）都只显示文案不报错

### 数据独立性（重要）
**客户端的 localStorage 与浏览器版完全独立**（不同 user-data 目录），已有数据迁移：旧环境「设置 → 关于 → 导出数据」→ 新环境「导入数据」。

## 八、给新会话 AI 的提示

1. 先读用户上传的 `专注计时器-源代码.zip` 里的 `index.html` 全文，以代码为准（本文档是地图，源码是领土）。
2. 改完功能务必跑回归：主流程、放弃弹窗（2 分钟/7 分钟两组用例）、连续专注开关开/关各跑一轮、主题切换锁、日志筛选、PWA 离线、检查更新按钮。
3. 测试环境注意：无头浏览器里 backdrop-filter 会把 FPS 压到 34 左右，属已知现象，不是回归。
4. **时间扭曲测试法**：页面计时基于 `Date.now()`，自动化测试时直接覆写 `Date.now` 注入偏移即可快进，不用等真时间；但注意 IIFE 内函数访问不到，只能 DOM 断言。
5. **连点防护**：主题切换有 460ms 防抖（v1.12.1 加的），其他按钮如果需要类似保护参照实现。
